En el caso de las migraciones, utilizaremos el comando "dotnet ef Migrations add [nombre migracion]".

Una vez que tengamos la migracion creada, tendremos que ejecutar el comando "dotnet ef database update".